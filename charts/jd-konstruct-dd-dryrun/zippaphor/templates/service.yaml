apiVersion: v1
kind: Service
metadata:
  name: {{ include "zippaphor.fullname" . }}
  labels:
    {{- include "zippaphor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zippaphor.selectorLabels" . | nindent 4 }}
